# 🛠️ Гайд по Налагодженню Помилок в HomieLife

## 📍 Де знайти помилки

### 1. **Terminal з Metro Bundler** (найважливіше)
```bash
cd /Users/yarchik/Homie/homie-app
npx expo start
```
Тут ви побачите:
- ❌ Помилки компіляції TypeScript
- ❌ Помилки імпортів модулів
- ⚠️ Попередження про застарілі пакети
- 🔴 Runtime errors з stack trace

### 2. **Expo Go App на телефоні**
Натисніть на помилку для детальної інформації:
- Red Screen of Death - критичні помилки
- Yellow Box - попередження
- Stack trace з номерами рядків

### 3. **Browser Console (для web)**
Відкрийте http://localhost:8081 та натисніть F12:
- Console tab - JavaScript помилки
- Network tab - помилки API запитів
- React DevTools - помилки компонентів

### 4. **React Native Debugger**
```bash
# Встановити якщо немає
brew install react-native-debugger

# В Expo Go натисніть "m" в терміналі
# Виберіть "Open Debugger"
```

## 📸 Як найкраще передавати помилки мені

### ✅ НАЙКРАЩИЙ ФОРМАТ:

1. **Повний текст помилки з терміналу:**
```
ERROR  TypeError: Cannot read property 'id' of undefined
    at TaskDetails (/Users/yarchik/Homie/homie-app/app/(modals)/task-details.tsx:45:23)
    at renderWithHooks (node_modules/react-native/Libraries/Renderer/...)
```

2. **Контекст помилки:**
- Що ви робили коли виникла помилка
- На якому екрані/компоненті
- Після якої дії (натискання кнопки, перехід на сторінку)

3. **Скріншоти (якщо UI проблема):**
- Red screen з Expo Go
- Некоректне відображення UI
- Проблеми з layout

### 🔍 Команди для збору інформації

```bash
# Перевірити логи Metro
npx expo start --clear

# Перевірити TypeScript помилки
npx tsc --noEmit

# Перевірити залежності
npm ls

# Перевірити версії пакетів
npm outdated
```

## 🎯 Типові помилки та їх місця

### Помилки Supabase/API:
- **Де:** Network tab в браузері або console.log в терміналі
- **Формат:**
  ```
  Error: Insert failed
  Code: 42501
  Message: new row violates row-level security policy
  ```

### TypeScript помилки:
- **Де:** Terminal при запуску або в IDE (VSCode)
- **Формат:**
  ```
  TS2339: Property 'nonExistent' does not exist on type 'User'
  ```

### Navigation помилки:
- **Де:** Red screen в Expo Go
- **Формат:**
  ```
  The action 'NAVIGATE' with payload {"name":"NonExistentScreen"} was not handled
  ```

### Async/State помилки:
- **Де:** Console в Expo Go або терміналі
- **Формат:**
  ```
  Warning: Can't perform a React state update on an unmounted component
  ```

## 📋 Чек-ліст при помилці

1. [ ] Скопіюйте повний текст помилки
2. [ ] Вкажіть файл і номер рядка
3. [ ] Опишіть кроки відтворення
4. [ ] Вкажіть на якому пристрої (iOS/Android/Web)
5. [ ] Якщо UI - додайте скріншот

## 🚀 Швидкі команди

```bash
# Очистити кеш і перезапустити
npx expo start -c

# Перевірити всі TypeScript помилки
npx tsc --noEmit --watch

# Логи в реальному часі з фільтром
adb logcat | grep -i "error\|exception"  # Android
xcrun simctl spawn booted log stream --level=error  # iOS

# Перевірити структуру бази даних
node verify-storage.js
```

## 💡 Приклади хороших звітів про помилки

### ✅ Добрий приклад:
```
Помилка при створенні таску:
ERROR TypeError: Cannot read property 'household_id' of null
  at CreateTask.tsx:89

Кроки:
1. Зайшов в Rooms
2. Натиснув Create Task
3. Заповнив форму
4. При натисканні Save - помилка

Пристрій: iPhone 14, Expo Go
```

### ❌ Поганий приклад:
```
не працює створення таску
```

## 🔧 Поточні відомі проблеми

1. **xcrun simctl help exited with non-zero code: 72**
   - Не критично, пов'язано з iOS симулятором
   - Не впливає на роботу на реальному пристрої

2. **expo-image-manipulator not found**
   - Тимчасово закоментовано стиснення фото
   - Фото завантажуються в оригінальному розмірі

## 📱 Доступ до логів

### Поточний Metro bundler:
http://localhost:8081

### Expo DevTools:
http://localhost:8081/_expo/debugger

### Ваш додаток в браузері:
http://localhost:8081