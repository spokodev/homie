# 🎯 Фінальні інструкції для локальних білдів HomieLife

## ✅ Що вже готово:
1. **Нативні проекти згенеровані** (`ios/` та `android/`)
2. **iOS залежності встановлені** (109 pods через CocoaPods)
3. **Xcode встановлений** на вашому Mac

## ❌ Що потрібно зробити ВАМ в терміналі:

### Для iOS (2 команди):
```bash
# 1. Налаштуйте Xcode (потрібен пароль):
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

# 2. Запустіть білд з правильним encoding:
cd /Users/yarchik/Homie/homie-app
export LANG=en_US.UTF-8 && export LC_ALL=en_US.UTF-8
npx expo run:ios --configuration Release
```

**Результат:** Отримаєте .app файл за 5-10 хвилин

### Для Android (3 команди):
```bash
# 1. Встановіть Java JDK:
brew install openjdk@17

# 2. Додайте в PATH:
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 3. Запустіть білд:
cd /Users/yarchik/Homie/homie-app/android
./gradlew assembleRelease
```

**Результат:** APK файл в `android/app/build/outputs/apk/release/app-release.apk`

## 📊 Порівняння з EAS:

| Метод | EAS (зараз) | Локально |
|-------|-------------|----------|
| **Черга** | 1+ година в черзі 😱 | 0 хвилин ✅ |
| **Швидкість білду** | 20-30 хв | 5-10 хв |
| **iOS білди** | Падають (RevenueCat) | Працюють |
| **Ціна** | $99/міс Priority | Безкоштовно |

## 🚨 Важливо:

1. **Для iOS:** Обов'язково виконайте `sudo xcode-select` перед білдом
2. **Для Android:** Без JDK білд не запуститься
3. **UTF-8:** Використовуйте export LANG команди для iOS

## 📱 Що робити з файлами:

### iOS .app → TestFlight:
1. Створіть архів в Xcode
2. Upload через Xcode Organizer
3. Або використайте Transporter app

### Android .apk → Встановка:
1. Відправте APK на телефон
2. Дозвольте "Install from unknown sources"
3. Встановіть і користуйтесь

## 💡 Альтернативи якщо не хочете встановлювати JDK/налаштовувати Xcode:

### GitHub Actions (безкоштовно):
- Створіть `.github/workflows/build.yml`
- 2000 хвилин/місяць безкоштовно
- Білди без черг

### Codemagic ($95/міс):
- Найпростіше налаштування (30 хв)
- Працює "з коробки"
- Без черг

## 📝 Підсумок:

**Зараз у вас:**
- ✅ iOS готовий до білду (потрібно тільки xcode-select)
- ⚠️ Android потребує JDK (5 хв встановити)
- ❌ EAS в черзі вже годину без результату

**Рекомендація:** Встановіть JDK через Homebrew (5 хвилин) і матимете повністю робочі локальні білди!

---

## Швидкі команди для копіювання:

### iOS (все в одному):
```bash
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer && \
cd /Users/yarchik/Homie/homie-app && \
export LANG=en_US.UTF-8 && export LC_ALL=en_US.UTF-8 && \
npx expo run:ios --configuration Release
```

### Android (після встановлення JDK):
```bash
cd /Users/yarchik/Homie/homie-app/android && \
./gradlew assembleRelease
```