export { BaseCard, type BaseCardProps } from './BaseCard';

// Specialized cards can be imported from existing screens for now
// TODO: Extract TaskCard, MemberCard, StatCard as reusable components
// when refactoring existing screens
