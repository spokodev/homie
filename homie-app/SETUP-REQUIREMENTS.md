# ⚙️ Що потрібно встановити для локальних білдів

## 📱 Для iOS білдів:

### ✅ У вас вже є:
- Mac комп'ютер
- Xcode (встановлений але не налаштований)
- Command Line Tools

### ❌ Потрібно налаштувати:
```bash
# Виконайте в терміналі (потрібен пароль):
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

# Перевірте:
xcodebuild -version
```

## 🤖 Для Android білдів:

### ✅ У вас вже є:
- Нативний проект Android (згенерований)
- Gradle (завантажився автоматично)

### ❌ Потрібно встановити Java JDK:

#### Варіант 1: Через Homebrew (рекомендовано, 5 хвилин)
```bash
# Встановіть JDK 17 (рекомендована версія для React Native):
brew install openjdk@17

# Додайте в PATH:
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Перевірте:
javac -version
```

#### Варіант 2: Завантажити з Oracle
1. Йдіть на https://www.oracle.com/java/technologies/downloads/
2. Завантажте JDK 17 для macOS ARM64
3. Встановіть .dmg файл
4. Перезапустіть термінал

#### Варіант 3: Через Android Studio
1. Завантажте Android Studio: https://developer.android.com/studio
2. Він автоматично встановить JDK
3. Але це займе більше місця (~10GB)

## 🚀 Після встановлення:

### Для iOS:
```bash
cd /Users/yarchik/Homie/homie-app
cd ios && pod install && cd ..
npx expo run:ios --configuration Release
```

### Для Android:
```bash
cd /Users/yarchik/Homie/homie-app/android
./gradlew assembleRelease
```

APK буде в: `android/app/build/outputs/apk/release/app-release.apk`

## ⚡ Швидка альтернатива:

Якщо не хочете встановлювати JDK/Xcode, використовуйте **GitHub Actions**:
- Безкоштовно для публічних репозиторіїв
- Або Codemagic ($95/міс але без налаштувань)

## 📊 Порівняння часу:

| Дія | Час |
|-----|-----|
| Встановити JDK через Homebrew | 5 хв |
| Налаштувати Xcode | 2 хв |
| Білд Android після налаштування | 5-10 хв |
| Білд iOS після налаштування | 5-10 хв |
| **Vs EAS в черзі** | **40+ хв** |

## 🎯 Рекомендація:

1. **Зараз:** Встановіть JDK через Homebrew (5 хвилин)
2. **Потім:** Налаштуйте Xcode (2 хвилини)
3. **Результат:** Необмежені локальні білди за 5-10 хвилин кожен!

---

**Головне:** Встановіть JDK і ваш Android білд запрацює!