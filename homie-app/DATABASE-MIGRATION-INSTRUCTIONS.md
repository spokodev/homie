# 🗄️ Інструкція для оновлення бази даних

## ⚠️ ВАЖЛИВО: Потрібно застосувати міграції до бази даних Supabase!

База даних ще **НЕ оновлена**. Код готовий, але потрібно виконати SQL міграції.

## 📝 Кроки для оновлення:

### 1. Відкрийте Supabase Dashboard
- Перейдіть на [app.supabase.com](https://app.supabase.com)
- Виберіть ваш проект HomieLife

### 2. Перейдіть в SQL Editor
- В лівому меню натисніть "SQL Editor"
- Натисніть "New Query"

### 3. Виконайте міграцію
- Скопіюйте весь вміст файлу: `apply-migrations.sql`
- Вставте в SQL Editor
- Натисніть "Run" (або Ctrl/Cmd + Enter)

### 4. Перевірте результати
В кінці скрипту автоматично виконається перевірка. Ви повинні побачити:
- ✅ 3 нові таблиці створені (task_categories, subtasks, task_photos)
- ✅ 3 нові колонки в таблиці tasks
- ✅ 6 нових колонок в таблиці recurring_tasks
- ✅ RLS політики створені для всіх нових таблиць

### 5. Створіть дефолтні категорії для існуючих households
Виконайте для кожного household_id:
```sql
SELECT create_default_categories('ваш-household-id-тут');
```

Або для всіх відразу:
```sql
DO $$
DECLARE
  h RECORD;
BEGIN
  FOR h IN SELECT id FROM households
  LOOP
    PERFORM create_default_categories(h.id);
  END LOOP;
END $$;
```

## ✅ Що буде після міграції:

1. **Категорії завдань** - динамічні, з бази даних
2. **Підзавдання** - можна додавати до будь-якого завдання
3. **Фото** - готова таблиця для завантаження фото
4. **Ротація** - гнучкі інтервали від хвилини до року

## ⚡ Перевірка роботи:

Після міграції перезапустіть додаток:
```bash
# Зупиніть Expo (Ctrl+C)
# Запустіть знову
npx expo start --tunnel
```

Тепер ви можете:
1. Створити нове завдання з підзавданнями
2. Адміни можуть створювати власні категорії
3. При виконанні завдання можна відмічати окремі підзавдання

## 🚨 Якщо щось пішло не так:

1. Перевірте логи в Supabase Dashboard → Logs → Database
2. Переконайтесь що всі таблиці існують:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
ORDER BY table_name;
```

3. Якщо потрібно відкотити зміни:
```sql
-- УВАГА: Це видалить всі нові дані!
DROP TABLE IF EXISTS task_photos CASCADE;
DROP TABLE IF EXISTS subtasks CASCADE;
DROP TABLE IF EXISTS task_categories CASCADE;
ALTER TABLE tasks DROP COLUMN IF EXISTS category_id;
ALTER TABLE tasks DROP COLUMN IF EXISTS completed_subtask_ids;
ALTER TABLE tasks DROP COLUMN IF EXISTS has_subtasks;
-- І так далі для recurring_tasks...
```

## 📊 Статус міграції:

- [ ] Скрипт `apply-migrations.sql` створений
- [ ] Міграція застосована в Supabase
- [ ] Дефолтні категорії створені
- [ ] Додаток перезапущений
- [ ] Функціонал протестований

---

**Файл для міграції:** `/homie-app/apply-migrations.sql`